package com.otis.scala.connectors

object PrintTable {
  def main(args: Array[String]): Unit = {
    //    CREATE TABLE print_table (
    //      f0 INT,
    //      f1 INT,
    //      f2 STRING,
    //      f3 DOUBLE
    //    ) WITH (
    //      'connector' = 'print'
    //    )

    //todo 或者使用like语句 创建和现有的表相同的schema
//    CREATE TABLE print_table WITH ('connector' = 'print')
//    LIKE source_table (EXCLUDING ALL)
  }
}
